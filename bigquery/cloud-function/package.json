{
  "name": "consent-logger",
  "version": "1.0.0",
  "description": "Cloud Function to log consent events to BigQuery",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "@google-cloud/bigquery": "^7.3.0",
    "uuid": "^9.0.1"
  },
  "scripts": {
    "deploy": "gcloud functions deploy logConsent --runtime nodejs20 --trigger-http --allow-unauthenticated --region asia-southeast3 --project conicle-ai-dev"
  }
}
